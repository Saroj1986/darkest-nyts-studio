#!/usr/bin/env bash
set -e
# Placeholder render pipeline. Install FFmpeg first.
# Example once audio and scene images exist:
# ffmpeg -loop 1 -i scene01.png -i voice.mp3 -c:v libx264 -tune stillimage -c:a aac -shortest output.mp4
echo "FFmpeg worker placeholder: connect generated scene assets + TTS audio here."
